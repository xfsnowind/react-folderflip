const windowSize = 3

const NUMBER_LIMITATION = 100000

export enum REDUCER_TYPE {
  EDGE,
  SHOWUP
}

export enum SECTION_STATE {
  STICKY,
  FLOAT
}

export default {
  SECTION_STATE,
  windowSize,
  NUMBER_LIMITATION
}
